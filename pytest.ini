[pytest]
testpaths = tests
addopts = -q
markers =
    gui: tests requiring PyQt6 and an offscreen Qt platform
filterwarnings =
    ignore:Setting the shape on a NumPy array has been deprecated in NumPy 2\.5.*:DeprecationWarning:joblib\.numpy_pickle

